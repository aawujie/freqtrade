# 🚀 Freqtrade 快速开始指南

## 📝 快捷命令脚本

已创建 `ft.sh` 脚本，封装了所有常用命令！

---

## 🎯 最常用的命令

### 启动交易
```bash
# ichiV1 策略
./ft.sh trade ichiV1

# 双均线策略
./ft.sh trade DoubleMAStrategy
```

### 回测
```bash
# 回测最近30天（默认）
./ft.sh bt ichiV1

# 回测指定时间范围
./ft.sh bt ichiV1 20240901-20240930

# 快速回测（最近7天）
./ft.sh bt-quick ichiV1
```

### 下载数据
```bash
# 下载单个交易对（最近30天）
./ft.sh dl BTC/USDT:USDT 30

# 下载配置文件中的所有交易对
./ft.sh dl-all
```

### 查看日志
```bash
# 查看最后100行
./ft.sh logs

# 实时查看
./ft.sh logs-live

# 只看错误
./ft.sh logs-error
```

### 生成图表
```bash
# 生成K线图
./ft.sh plot ichiV1 BTC/USDT:USDT

# 生成收益图
./ft.sh plot-profit ichiV1
```

---

## 📚 完整命令列表

### 📊 交易命令
```bash
./ft.sh trade <策略>        # 启动交易
./ft.sh stop               # 停止交易
./ft.sh status             # 查看状态
```

### 🔙 回测命令
```bash
./ft.sh bt <策略> [时间]    # 回测
./ft.sh bt-quick <策略>    # 快速回测（7天）
```

### 💾 数据命令
```bash
./ft.sh dl <交易对> [天数]  # 下载数据
./ft.sh dl-all            # 下载所有
./ft.sh list-data         # 查看数据
```

### 📈 绘图命令
```bash
./ft.sh plot <策略> <对>   # K线图
./ft.sh plot-profit <策略> # 收益图
```

### 📝 日志命令
```bash
./ft.sh logs [行数]        # 查看日志
./ft.sh logs-live         # 实时日志
./ft.sh logs-error        # 错误日志
```

### 🔍 查询命令
```bash
./ft.sh show-config       # 显示配置
./ft.sh list-strategies   # 列出策略
./ft.sh show-trades       # 交易记录
```

### 🛠️ 工具命令
```bash
./ft.sh shell             # 进入容器
./ft.sh restart           # 重启容器
./ft.sh version           # 查看版本
```

---

## 💡 使用技巧

### 1. 查看帮助
```bash
./ft.sh help
```

### 2. 策略名称简写
脚本支持策略名称简写：
- `ichiV1` ✅
- `DoubleMAStrategy` 或 `double_ma` ✅

### 3. 自动选择配置文件
脚本会根据策略名自动选择对应的配置文件：
- `ichiV1` → `config_ichiV1.json`
- `DoubleMAStrategy` → `config_double_ma.json`

### 4. 组合使用
```bash
# 完整工作流
./ft.sh dl-all                              # 1. 下载数据
./ft.sh bt ichiV1 20240901-20240930         # 2. 回测
./ft.sh plot ichiV1 BTC/USDT:USDT           # 3. 绘图
./ft.sh trade ichiV1                        # 4. 启动交易
./ft.sh logs-live                           # 5. 实时监控
```

---

## 📋 典型使用场景

### 场景 1：新策略测试
```bash
# 1. 下载最近30天数据
./ft.sh dl BTC/USDT:USDT 30

# 2. 快速回测验证
./ft.sh bt-quick ichiV1

# 3. 详细回测
./ft.sh bt ichiV1 20240901-20240930

# 4. 查看结果
./ft.sh show-trades ichiV1

# 5. 生成图表分析
./ft.sh plot ichiV1 BTC/USDT:USDT
```

### 场景 2：日常交易监控
```bash
# 1. 启动交易
./ft.sh trade ichiV1

# 2. 实时监控（新终端）
./ft.sh logs-live

# 3. 查看状态
./ft.sh status

# 4. 查看交易记录
./ft.sh show-trades ichiV1
```

### 场景 3：数据更新
```bash
# 更新所有配置中的交易对数据
./ft.sh dl-all

# 查看已下载的数据
./ft.sh list-data
```

### 场景 4：故障排查
```bash
# 1. 查看错误日志
./ft.sh logs-error

# 2. 查看详细日志
./ft.sh logs 500

# 3. 进入容器调试
./ft.sh shell

# 4. 重启容器
./ft.sh restart
```

---

## 🎨 别名设置（可选）

如果觉得 `./ft.sh` 还是太长，可以设置别名：

### 添加到 ~/.zshrc 或 ~/.bashrc
```bash
# Freqtrade 快捷命令
alias ft='~/code/freqtrade-ws/ft.sh'
```

### 重新加载配置
```bash
source ~/.zshrc  # 或 source ~/.bashrc
```

### 使用别名
```bash
ft trade ichiV1
ft bt DoubleMAStrategy
ft logs-live
ft status
```

---

## 🔥 高频命令速查

```bash
# 最常用的5个命令
./ft.sh trade ichiV1        # 启动交易
./ft.sh status              # 查看状态
./ft.sh logs-live           # 实时日志
./ft.sh bt ichiV1           # 回测
./ft.sh dl-all              # 下载数据
```

---

## ⚡ 快捷键建议

在终端创建这些别名，更快捷：

```bash
# 添加到 ~/.zshrc
alias ft-trade='./ft.sh trade ichiV1'
alias ft-bt='./ft.sh bt ichiV1'
alias ft-logs='./ft.sh logs-live'
alias ft-status='./ft.sh status'
alias ft-dl='./ft.sh dl-all'
```

使用：
```bash
ft-trade    # 直接启动交易
ft-logs     # 查看实时日志
ft-status   # 查看状态
```

---

## 🎯 总结

| 操作 | 命令 |
|------|------|
| **启动交易** | `./ft.sh trade ichiV1` |
| **回测** | `./ft.sh bt ichiV1` |
| **下载数据** | `./ft.sh dl-all` |
| **查看日志** | `./ft.sh logs-live` |
| **查看状态** | `./ft.sh status` |
| **生成图表** | `./ft.sh plot ichiV1 BTC/USDT:USDT` |

**现在只需要记住一个脚本：`./ft.sh`** 🎉

查看所有命令：`./ft.sh help`
