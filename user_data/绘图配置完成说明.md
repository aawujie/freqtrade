# ✅ 绘图配置完成说明

## 📊 新增绘图配置文件

已成功将绘图配置模块化并移动到 `configs/` 文件夹，新增了4个专业的绘图配置：

```
configs/
├── config_plot.json              ✅ 通用完整绘图配置
├── config_plot_minimal.json      ✅ 最简绘图配置
├── config_plot_double_ma.json    ✅ 双均线策略专用
└── config_plot_ichimoku.json     ✅ 一目均衡策略专用
```

## 🎯 配置文件特点

### 1. config_plot.json - 通用完整配置
**包含指标**：
- 主图：SMA20/50, EMA50/100/200, 布林带
- 子图：成交量、RSI、MACD、随机指标

**适用场景**：通用策略分析、多指标对比

### 2. config_plot_minimal.json - 最简配置
**包含指标**：
- 主图：EMA50, EMA200
- 子图：成交量、RSI

**适用场景**：快速查看、性能优先

### 3. config_plot_double_ma.json - 双均线专用
**包含指标**：
- 主图：快线、慢线、趋势线、布林带
- 子图：成交量（含均线）、RSI、MACD

**适用场景**：DoubleMAStrategy 策略分析

### 4. config_plot_ichimoku.json - 一目均衡专用
**包含指标**：
- 主图：一目均衡云图、转换线、基准线、8条趋势线
- 子图：fan_magnitude、fan_magnitude_gain

**适用场景**：ichiV1 策略分析

## 🚀 快速使用

### 基础绘图命令

```bash
# 使用通用配置
docker compose run --rm freqtrade plot-dataframe \
  --config /freqtrade/user_data/configs/config_plot.json \
  --strategy DoubleMAStrategy \
  --pairs BTC/USDT \
  --timerange 20240901-20240930
```

### 策略专用绘图

```bash
# 双均线策略
docker compose run --rm freqtrade plot-dataframe \
  --config /freqtrade/user_data/configs/config_plot_double_ma.json \
  --strategy DoubleMAStrategy \
  --pairs BTC/USDT \
  --timerange 20240901-20240930

# 一目均衡策略
docker compose run --rm freqtrade plot-dataframe \
  --config /freqtrade/user_data/configs/config_plot_ichimoku.json \
  --strategy ichiV1 \
  --pairs BTC/USDT:USDT \
  --timerange 20240901-20240930
```

### 在 Jupyter Notebook 中使用

```python
# Cell: 绘制双均线策略图表
!docker compose run --rm freqtrade plot-dataframe \
  --config /freqtrade/user_data/configs/config_plot_double_ma.json \
  --strategy DoubleMAStrategy \
  --pairs BTC/USDT \
  --timerange 20240901-20240930
```

## 💡 组合配置使用

可以组合多个配置文件：

```bash
# 基础配置 + 交易对配置 + 绘图配置
docker compose run --rm freqtrade plot-dataframe \
  -c /freqtrade/user_data/configs/config_base.json \
  -c /freqtrade/user_data/configs/config_pairs_spot.json \
  -c /freqtrade/user_data/configs/config_plot.json \
  --strategy DoubleMAStrategy \
  --pairs BTC/USDT
```

## 📋 完整的绘图工作流

### 1. 下载数据
```bash
docker compose run --rm freqtrade download-data \
  --config /freqtrade/user_data/configs/config_base.json \
  --timerange 20240901-20240930 \
  --timeframe 5m
```

### 2. 运行回测
```bash
docker compose run --rm freqtrade backtesting \
  --config /freqtrade/user_data/config_composed.json \
  --timeframe 5m \
  --timerange 20240901-20240930
```

### 3. 绘制数据图表
```bash
docker compose run --rm freqtrade plot-dataframe \
  --config /freqtrade/user_data/configs/config_plot_double_ma.json \
  --strategy DoubleMAStrategy \
  --pairs BTC/USDT \
  --timerange 20240901-20240930
```

### 4. 绘制盈利图表
```bash
docker compose run --rm freqtrade plot-profit \
  --config /freqtrade/user_data/config_composed.json \
  --strategy DoubleMAStrategy \
  --timerange 20240901-20240930
```

## 📁 输出文件位置

图表文件保存在：
```
user_data/plot/
├── freqtrade-plot-BTC_USDT-5m.html      ← K线和指标图
├── freqtrade-plot-profit-*.html         ← 盈利曲线图
└── ...
```

用浏览器打开 HTML 文件即可查看交互式图表！

## 🎨 配置文件亮点

### 1. 完善的颜色方案
- 使用专业的配色方案，易于区分不同指标
- 支持半透明效果，避免遮挡

### 2. 策略专用优化
- 每个策略有专门优化的绘图配置
- 只显示策略相关的指标，避免混乱

### 3. 详细的注释
- 每个配置文件都有清晰的注释
- 包含使用示例和说明

### 4. 灵活组合
- 可以单独使用或组合使用
- 支持覆盖和扩展

## 📚 详细文档

查看完整的绘图配置文档：
- [绘图配置说明.md](configs/绘图配置说明.md) - 详细使用教程
- [绘图使用指南](../docs/07-绘图使用指南.md) - Freqtrade 绘图指南

## 🔄 迁移说明

### 旧配置文件处理
原有的 `config_plot.json` 已备份为 `config_plot.json.old`，新配置文件功能更完善。

### 更新你的脚本
如果你在脚本或 Notebook 中使用了绘图命令，请更新路径：

```bash
# 旧路径
--config /freqtrade/user_data/config_plot.json

# 新路径
--config /freqtrade/user_data/configs/config_plot.json
# 或使用策略专用配置
--config /freqtrade/user_data/configs/config_plot_double_ma.json
```

## ✅ 配置清单

现在 `configs/` 文件夹包含的所有模块：

- [x] ✅ config_base.json - 基础配置
- [x] ✅ config_secrets.json - 敏感信息
- [x] ✅ config_pairs_spot.json - 现货交易对
- [x] ✅ config_pairs_futures.json - 合约交易对
- [x] ✅ config_plot.json - 通用绘图
- [x] ✅ config_plot_minimal.json - 最简绘图
- [x] ✅ config_plot_double_ma.json - 双均线绘图
- [x] ✅ config_plot_ichimoku.json - 一目均衡绘图

## 🎉 完成！

所有配置文件已完美模块化！现在你可以：

1. ✅ 使用统一的 configs 文件夹管理所有配置
2. ✅ 根据不同策略选择合适的绘图配置
3. ✅ 灵活组合多个配置文件
4. ✅ 享受专业的可视化分析体验

开始可视化你的交易策略吧！📊🚀
